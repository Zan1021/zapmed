# Copy this to .env when using Docker (docker-compose up -d)
DB_CONNECTION=pgsql
DB_HOST=127.0.0.1
DB_PORT=5432
DB_DATABASE=zapmed
DB_USERNAME=zapmed
DB_PASSWORD=zapmed_secret

CACHE_STORE=redis
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASSWORD=null
